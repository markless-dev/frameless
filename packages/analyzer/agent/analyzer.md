# Analyzer package playbook

Keep `src/` portable and framework-free. React, Solid, browser providers, JSX transforms, DOM
event dispatch helpers, and handwritten calibration references belong to the consuming framework
packages. Import analyzer contract constants and data exports instead of repeating protocol facts.

# GoalBuddy v2 state.yaml
# Board truth lives here. goal.md is the editable charter; notes/ holds long receipts only.

version: 2

goal:
  title: "Frameless v0: the real product"
  slug: "frameless-product-v0"
  kind: existing_plan
  tranche: "Turn the proven POC pipeline into the real Frameless product: a monorepo with a compiler-extension layer consuming the markless compiler, a targets/ architecture (React 19, Solid), the equivalence oracle as product test infrastructure, and one end-to-end build command demonstrated on a real demo component library with receipts."
  status: active
  oracle:
    signal: "From a fresh checkout, one documented command compiles the demo TSRX component library into React 19 and Solid packages that (a) pass each target's conventionality gate, (b) produce green cross-target equivalence receipts (React output vs Solid output behaviorally equal under the oracle in headless Chromium, per scripted scenarios), and (c) the migrated POC evidence suites still pass."
    cadence: "after each Worker package and at final audit"
    final_proof: "Final Judge audit runs the fresh-checkout end-to-end flow plus all suites and records full_outcome_complete: true."
  intake:
    original_request: "I want the real frameless project, in reality — something that would change the game for frontend forever. Decide: compiler extension vs targets folder; vitest browser mode per framework for runtime checking; what all should we do."
    interpreted_outcome: "Frameless exists as a consumable product skeleton: compiler-extension layer + per-target packages + oracle harness + build entry, proven end-to-end on a real demo library with equivalence receipts."
    input_shape: existing_plan
    audience: "The user (markless/Frameless author); next tranche audiences: library authors evaluating Frameless."
    authority: requested
    proof_type: test
    completion_proof: "One documented command builds the demo library to both targets with gates + receipts green from a fresh checkout; POC evidence suites remain green; final audit records full_outcome_complete: true."
    likely_misfire: "Scope explosion driven by 'change the game forever' energy (composition/SSR/more targets creeping into v0); a grand rewrite that discards proven POC code instead of migrating it; or skeleton theater — a beautiful folder structure with no working end-to-end command."
    blind_spots_considered:
      - "The markless fixing board owns the markless repo right now — this goal must not touch it; IR-upstreaming waits for that board to close."
      - "React target bump 18.3.1 -> 19 changes the ref story and possibly act/testing semantics; must be validated, not assumed."
      - "The demo library has no hand-written references — its receipts are CROSS-TARGET equivalence (React vs Solid outputs) plus scenario scripts; that is the product's actual value prop and must be exercised."
      - "npm naming unresolved (frameless is squatted/stale) — publishing is out of scope; package names internal until resolved."
      - "The POC packages are the evidence base for the shipped report — migration must not break their suites."
    existing_plan_facts:
      - "Architecture (user-confirmed): compiler extension AND targets folder at different layers — the enriched IR (poc/05, frameless-enriched-ir contract) is the compiler-extension pass consuming markless's semantic graph + TSRX AST; targets/react and targets/solid each own emitter + conventionality-gate config + oracle adapter + framework-version matrix."
      - "Runtime verification: vitest browser mode per framework (poc/08 pattern) becomes the product test harness; receipts become the per-release support matrix."
      - "React target: 19 (ref-as-prop, no forwardRef). Solid: v2-forward idioms, v1 runtime fallback documented until the v2 toolchain exists."
      - "Write-on-change is regular state (user decision) — no sink primitive needed this tranche; mount-scoped work is a markless-language question queued behind the markless fixing board."
      - "Consume markless via pinned vendored artifacts (poc/vendor pattern) until the fixing board closes; then re-pack and re-verify."
      - "Composition surface (children/slots, shared->context, refs) is the NEXT tranche, designed per the recorded discussion; v0 covers the proven fixture-family surface."
      - "Migrate proven POC code; do not rewrite from scratch. POC packages stay intact as evidence."
      - "2026-07-19 user constraint: per-target IDIOM DOSSIERS — before each target package, a corpus-evidenced research pass (grep MCP if connected, framework docs, and the local framework repos under /Users/jacksm5pro/dev/open-source e.g. solid/, qwik/) records the most idiomatic pattern per IR construct (e.g. derived state: Solid createMemo; React useMemo-vs-derived-in-render decided with evidence and thresholds), each decision with citations; the emitter implements the dossier and the conventionality gate enforces it. Verification is layered: (1) IR/compiler contract tests, (2) emitter golden/freshness tests, (3) conventionality gate (build+lint+AST policies from the dossier), (4) browser-mode runtime equivalence via the oracle, (5) fresh-checkout end-to-end."
      - "2026-07-19 user constraint: monorepo conventions MUST match the markless repo (it will be pushed to the markless GitHub org): pnpm workspace layout under packages/, package.json/tsconfig shapes, test lanes and doctor-script pattern, specs/ discipline, CONTRIBUTING package map, agent playbook file pattern (agent/<name>.md), and tooling choices (vitest, vite/rolldown) mirrored from /Users/jacksm5pro/dev/open-source/markless — read-only reference."

rules:
  pm_owns_state: true
  one_active_task: true
  max_write_workers: 1
  no_implementation_without_worker_or_pm_task: true
  no_completion_without_judge_or_pm_audit: true
  planning_is_not_completion: true
  queued_required_worker_blocks_completion: true
  continuous_until_full_outcome: true
  missing_input_or_credentials_do_not_stop_goal: true
  preserve_and_validate_existing_plan: true
  intake_misfire_must_be_audited: true
  goal_pressure_requires_oracle: true
  no_completion_on_weak_proof: true
  slice_policy:
    max_consecutive_tiny_tasks: 2
    prefer_vertical_slices: true
    judge_picks_largest_safe_slice: true
    worker_completes_whole_slice: true

agents:
  # installed | bundled_not_installed | missing | unknown
  scout: installed
  worker: installed
  judge: installed

visual_board:
  # none | local | unknown
  selected: local
  local:
    status: live
    url: "http://goalbuddy.localhost:41737/frameless-product-v0/"
    command: "npx goalbuddy board docs/goals/frameless-product-v0"

active_task: T007

tasks:
  - id: T001
    type: judge
    assignee: Judge
    status: done
    reasoning_hint: high
    objective: "Architecture lock: derive the markless repo's monorepo conventions (workspace layout, package.json/tsconfig shapes, test lanes, doctor script, specs/ + CONTRIBUTING + agent-playbook patterns) and lock the Frameless layout to match them; then decide the frameless-enriched-ir contract ownership and version, the exact Target interface (emitter, gate config, oracle adapter, version matrix per targets/<name>), the build entry (CLI vs vite plugin first), the React 19 migration plan, and the migrate-vs-rewrite map for each poc package. Cut the first Worker packages."
    inputs:
      - "/Users/jacksm5pro/dev/open-source/markless (read-only: workspace layout, package manifests, tsconfigs, test/doctor scripts, specs/, CONTRIBUTING.md, packages/*/agent/*.md)"
      - "docs/goals/frameless-mitosis-successor/notes/T004-claims.md (all adjudications + addenda)"
      - "poc/04..08 READMEs and source structure"
      - "goal.intake.existing_plan_facts"
    constraints:
      - "Read-only; decisions with rationale, not implementation."
      - "Validate the user-confirmed architecture against evidence; flag any contradiction rather than silently complying."
      - "Every Worker package it cuts gets allowed_files, verify, stop_if."
    expected_output:
      - "notes/T001-architecture.md: locked decisions + Target interface spec + migration map + Worker package definitions"
    receipt:
      result: done
      decision: "approved-with-amendments"
      note: notes/T001-architecture.md
      summary: "Judge derived markless conventions from the checkout and locked layout/IR//1-deferral/CLI-first/React-19 plan with 3 charter flags (flat target packages, doctor placement, oracle-before-emitters resequencing). Crew critique REJECTED the initial lock: compiler-owned Target contract was a dependency cycle; worker packets untranscribed; convention over-generalizations; /1 deferral needed explicit v0 limits (one component/file, no .d.ts, no sourcemaps). PM adjudicated all amendments into the note (ownership split, pipeline trichotomy, per-POC frozen-install proof, T008 split). Merge review: read-only judge+critique, no branches; critique WAS the second model."
  - id: T002
    type: scout
    assignee: Scout
    status: done
    reasoning_hint: medium
    objective: "React 19 idiom dossier: corpus-evidenced pattern decisions per IR construct (state, derived/memo thresholds, refs-as-props, events, keyed lists, conditionals, once-per-instance setup) using grep MCP if connected, React docs, and local corpora; every decision cited; output feeds the targets/react emitter and gate."
    inputs:
      - "poc/06-emit-react mapping table + gate (current decisions to confirm or overturn)"
      - "React 19 docs; grep MCP / local corpora"
    constraints:
      - "Read-only. Every pattern decision carries evidence (corpus frequency, docs citation, or maintainer guidance) — no taste-only calls."
    expected_output:
      - "notes/T002-react-idioms.md: construct -> chosen idiom -> evidence -> gate rule"
    receipt:
      result: done
      note: notes/T002-react-idioms.md
      summary: "11 construct rulings, all cited (react.dev live-fetched, hooks-lint v6, corpus). Overturns: onInput->onChange, let-rebind->const SSA + one-set-per-cell, literal initials unwrapped, ref-guard null-shape. Confirms: derived-in-render (useMemo never in v0, threshold recorded), useRef counters, keyed idioms, ternary-null. Resolves T001 act decision: ASYNC act (React deprecation commitment) -> adapter migration + full mutant recalibration under 19 pins. 6 splits threshold-picked with overturn triggers. Gate diffs enumerated (ESLint 9 flat + hooks v6). Critique skipped: research receipt with per-claim citations; enforcement lands in T005 which gets full critique."
  - id: T003
    type: scout
    assignee: Scout
    status: done
    reasoning_hint: medium
    objective: "Solid idiom dossier: same protocol for Solid (createMemo for derived, store-vs-signal thresholds, For/Show usage, props access patterns, v2-forward idioms), confirming or overturning poc/07 decisions and addressing the row-reactivity critique findings with evidenced patterns."
    inputs:
      - "poc/07-emit-solid mapping table + generality-boundary docs"
      - "Solid docs (1.x + 2.0 experimental); grep MCP / local corpora incl. /Users/jacksm5pro/dev/open-source/solid"
    constraints:
      - "Read-only. Every pattern decision carries evidence."
    expected_output:
      - "notes/T003-solid-idioms.md: construct -> chosen idiom -> evidence -> gate rule"
    receipt:
      result: done
      note: notes/T003-solid-idioms.md
      summary: "11 rulings, all cited (docs.solidjs.com live, solid v2.0.0-beta.9 local checkout, benchmark corpora, 1.8.22 runtime dist). Headline: createStore for S2 todos answers the W-C2 row-reactivity blocker structurally (bare row reads, produce edits, reconcile-keyed structural updates lowering the IR key); createMemo OVERTURNS the prior expectation — plain derived arrows confirmed by docs+corpus for single-subscriber derivations; Show-with-siblings-once is the S2 duplication fix, calibration-receipt-proven; attr:value kept with runtime Properties receipt; untrack once-captures as v2-forward hardening; full v2 ledger (For accessor children, attr: removal, microtask batching recalibration). 7 splits with overturn triggers. Critique skipped: research receipt, per-claim citations; enforcement lands in T006 with full critique."
  - id: T004
    type: worker
    assignee: Worker
    status: done
    reasoning_hint: medium
    objective: "Scaffold the product monorepo per the ADJUDICATED T001 note (ownership split; convention corrections) and migrate the enriched IR into packages/compiler with contract tests green; NO cross-package contract work. Carries the critique's verbatim instruction block (note section 9)."
    allowed_files:
      - "package.json, pnpm-workspace.yaml, pnpm-lock.yaml, vite.config.ts, tsconfig.json, LICENSE, AGENTS.md, CONTRIBUTING.md, README.md, .gitignore, .ruler/**, .githooks/**"
      - "vendor/**, specs/**, packages/compiler/**"
      - "stub manifests + src/index.ts for packages/{oracle,target-react,target-solid,cli} and demos/ui-kit/package.json"
    verify:
      - "pnpm install (frozen after first lock); pnpm check; pnpm build (vp pack); pnpm test (compiler suite green)"
      - "vendored tarball sha256 == poc/08 receipts; cd poc/05-enriched-ir && pnpm test green; git diff --stat poc/ empty"
    stop_if:
      - "Need files outside allowed_files."
      - "Adjudicated note ambiguous on a needed decision."
      - "Declarations/type-preserving output/sourcemaps turn out required — reopen IR decision, do not hack."
      - "A markless convention cannot be mirrored without touching poc/."
      - "Verification fails twice."
    receipt:
      result: done
      summary: "Monorepo scaffolded per adjudicated T001: root workspace (markless lanes + catalogs), vite-plus config with poc/** excluded from all lanes, MIT license, .ruler/AGENTS/CONTRIBUTING/README, vendor/ with sha256 receipts (found+corrected a checksum typo in poc/08's recorded receipt — erratum, poc file untouched), specs/framework/00-05 incl. ownership-split target contract, packages/compiler with poc/05 migrated (byte-identical /1 goldens, fail-closed v0 limits, agent/frameless.md playbook), stubs for oracle/targets/cli/demo. PM verification: frozen install, tsc, vp pack, 23/23 product, 18/18 poc/05, checksums OK, poc/ diff empty. Critique skipped: scaffold executed the already-critiqued adjudicated architecture verbatim; targets/oracle get their own critiques."
      changed_files:
        - "root scaffold + vendor/ + specs/ + packages/compiler (see merge t004)"
      commands:
        - cmd: "pnpm install --frozen-lockfile && pnpm check && pnpm build && pnpm test; poc/05 pnpm test; shasum -c"
          status: pass
  - id: T005
    type: worker
    assignee: Worker
    status: queued
    reasoning_hint: medium
    objective: "targets/react: migrate the poc/06 emitter + hardened gate into the Target interface, implement the T002 idiom dossier (emitter emits the dossier patterns; gate enforces them), execute the React 19 bump per T001 plan; all layers green."
    allowed_files: []
    verify: []
    stop_if:
      - "Need files outside allowed_files."
      - "React 19 bump breaks oracle/adapter semantics in a way T001 did not anticipate — report, do not improvise."
      - "Verification fails twice."
    receipt: null
  - id: T006
    type: worker
    assignee: Worker
    status: queued
    reasoning_hint: medium
    objective: "targets/solid: migrate the poc/07 structural emitter (fail-closed soundness + generality boundary docs) into the Target interface and implement the T003 idiom dossier (incl. createMemo for derived); all layers green."
    allowed_files: []
    verify: []
    stop_if:
      - "Need files outside allowed_files."
      - "Verification fails twice."
    receipt: null
  - id: T007
    type: worker
    assignee: Worker
    status: active
    reasoning_hint: medium
    objective: "oracle package (RESEQUENCED before targets per T001 adjudication): migrate poc/04 oracle core + poc/08 harness into @frameless/oracle — Adapter/scenario/trace/verdict/receipt ownership per the ownership split; frameless-receipts/1 schema; browser project wired into root vite config; framework adapters STAY OUT (they move to target packages later); handwritten references kept as calibration fixtures; full mutant calibration green in headless Chromium under the product lane."
    allowed_files:
      - "packages/oracle/**"
      - "root vite.config.ts (browser project + pack entry), root package.json (scripts)"
    verify:
      - "pnpm check; pnpm build; pnpm test (node lane: compare/serialize/verdict units)"
      - "browser lane: calibration green (references equal, ALL mutant classes rejected) in headless Chromium"
      - "cd poc/04-equivalence-oracle && pnpm test green; poc/ diff empty"
    stop_if:
      - "Need files outside allowed_files."
      - "Browser listener denied in sandbox — complete code, hand browser lane to PM, do not fake."
      - "Adapter/framework types would need to enter the oracle package — ownership-split violation, stop and report."
      - "Verification fails twice."
    receipt: null
  - id: T008
    type: worker
    assignee: Worker
    status: queued
    reasoning_hint: medium
    objective: "Build entry + demo: implement the T001-chosen build entry (CLI or vite plugin) and a small real demo TSRX component library within the proven surface; end-to-end: demo lib -> React 19 + Solid packages passing gates + green cross-target equivalence receipts."
    allowed_files: []
    verify: []
    stop_if:
      - "Need files outside allowed_files."
      - "Demo library needs constructs outside the proven surface — trim the demo, do not extend the surface in this tranche."
      - "Verification fails twice."
    receipt: null
  - id: T009
    type: judge
    assignee: Judge
    status: queued
    reasoning_hint: high
    objective: "End-to-end boundary review: architecture as built vs T001, receipts honesty, gate strength, migration completeness; decide remaining work before final audit."
    receipt: null
  - id: T999
    type: judge
    assignee: Judge
    status: queued
    reasoning_hint: high
    objective: "Final audit: fresh-checkout end-to-end (documented command -> demo lib -> both targets -> gates + receipts green), all migrated suites + POC evidence suites green, oracle signal fully satisfied; decide full_outcome_complete."
    inputs:
      - "All done task receipts"
      - "Last verification"
      - "Current dirty diff"
    constraints:
      - "Do not implement."
      - "Reject completion if the end-to-end command does not work from a fresh checkout."
      - "Reject completion if any POC evidence suite broke."
      - "Audit the recorded likely-misfire explicitly (scope explosion, rewrite-instead-of-migrate, skeleton theater)."
    expected_output:
      - "complete | not_complete"
      - "full_outcome_complete: true | false"
      - "missing evidence"
      - "next task if not complete"
    receipt: null

checks:
  dirty_fingerprint: unknown
  last_verification:
    result: unknown
    task: null
    commands: []

# GoalBuddy v2 state.yaml
# Board truth lives here. goal.md is the editable charter; notes/ holds long receipts only.
#
# PREPPED, NOT STARTED. Created by frameless-idiom-policy-v1 task T007 on 2026-07-26.

version: 2

goal:
  title: "Frameless Svelte adapter v1"
  slug: "frameless-svelte-v1"
  kind: specific
  tranche: "Land a Svelte 5 lane end to end - official scaffold, emitter, gate, goldens, demo, e2e row asserting S1/S2/S3 equality - then rule on IR-4, the target-framework-version input the whole policy's version corollary depends on."
  status: active
  autonomy_grant:
    granted: "2026-07-27, by the owner: continuous execution to goal end, no check-ins."
    scope: >-
      Rulings previously framed as needing the owner - notably whether demos/svelte-official is a
      SvelteKit app or a bare vite+plugin-svelte setup - are Judge calls, to be ruled on evidence
      and recorded. Do not defer upward.
    not_covered: >-
      Outward-facing publication under the owner's name, force-pushing, or rewriting published
      history.
  prepped_not_started: >-
    NOTHING IS RUNNING THIS BOARD. status reads active and active_task reads T001 only because the
    GoalBuddy checker requires exactly one active task while a goal is active; there is no
    alternative encoding for "prepped". Every task receipt is null, which is the reliable signal.
    Revalidated by frameless-defects-and-targets-v1 T010 on 2026-07-27.
  inherited_gaps_revalidated: >-
    Two of the three standing gaps inherited from frameless-idiom-policy-v1 are now FIXED and the
    charter has been corrected: pnpm check DOES typecheck emitted output (three tsc passes plus a
    per-package emitted-typecheck lane), and pnpm test:browser runs clean (react 55/55, solid
    44/44). Still true: pnpm e2e pins dev mode only, scenarios.box.ts modes ['dev'].
  instrument_rules: >-
    THREE STANDING CONSTRAINTS added by T010, full text in goal.md. This board adds four new
    instruments and four of six findings on the defects goal were instrument faults. (1) Two-variable
    triangulation before any finding is filed - and a finding that reproduces on a stock official
    scaffold with none of our code is evidence the TEST is unfair, not that the framework is broken.
    (2) Every instrument asserts its own preconditions. (3) Two-sided calibration for harnesses, not
    only gates - a settle loop that cannot throw is not a settle loop.
  oracle:
    signal: "pnpm e2e includes a Svelte row driving an official Svelte scaffold at the pinned lockfile version, and its S1/S2/S3 observations are ASSERTED equal to demos/react-official, demos/solid-official and demos/qwik under the same matrix."
    cadence: "after each Worker package and at final audit"
    final_proof: "A green pnpm e2e receipt naming the Svelte row, plus an explicit IR-4 ruling: either the target-version input is implemented and at least one version-gated sugar question resolves through it, or IR-4 is deferred with the reason recorded AND the consequence for the Vue and Angular boards stated."
  intake:
    original_request: "Owner asked, during frameless-idiom-policy-v1: 'I'm also wondering if we should start the goals for Vue, Angular, and Svelte.' This board is the Svelte answer, prepped after the idiom policy was ratified."
    interpreted_outcome: "Svelte 5 becomes a frameless target with proven activation neutrality on official tooling, and the version-pinning problem it surfaces gets a decision rather than a workaround."
    input_shape: specific
    audience: "frameless maintainers and future adapter authors"
    authority: requested
    proof_type: test
    completion_proof: "Svelte row green in pnpm e2e with asserted S1/S2/S3 equality, plus an explicit IR-4 ruling."
    likely_misfire: "Implementing bind: because it looks like the idiomatic Svelte spelling, without noticing it is different-in-kind on two axes - and a wrong bind: surfaces as a DEV-ONLY console warning, so it can pass tests and be wrong in production."
    blind_spots_considered:
      - "Svelte is where the version pin stops being a detail. onclick (Svelte 5) vs on:click (Svelte 4) is the SAME lexical substitution flipping category across a major; reassignable $derived needs 5.25+; and Svelte 5 REMOVED event modifiers, which interacts directly with IR-5. The policy found that same-in-kind is a property of a sugar PLUS a pinned version, and this is where that bites hardest."
      - "IR-4 (no target-framework-version input) is arguably this board's most consequential decision, and the other two adapter boards inherit whatever it decides. The policy's version corollary explicitly names adding that input as an adapter-board concern - this is that board."
      - "A wrong bind: call fails as a dev-only ownership_invalid_mutation console warning, not a build error. Absence of a red test is not evidence of correctness here."
    existing_plan_facts:
      - "docs/emitter-idiom-policy.md governs every emission-site form choice. Run the six gates; record outcomes."
      - "Official scaffold only (npx sv create or current official equivalent), as it ships. No hand-rolled harness - see docs/goals/frameless-qwik-v1/."
      - "IR gaps biting Svelte: IR-1 no bindable prop kind (blocks $bindable/bind:), IR-3 default slot only (blocks snippets), IR-4 no framework-version input (blocks reassignable $derived 5.25+ and the onclick/on:click split itself), IR-5 modifier vocabulary is exactly preventDefault|stopPropagation while Svelte 5 REMOVED modifiers, IR-6 no class/style binding vocabulary (blocks class: directives), IR-7 purity never asserted while $derived expects a pure expression."
      - "ALREADY SURVEYED: bind: is DIFFERENT-IN-KIND from the Qwik $ case on two independent axes - it requires an LVALUE (the IR's path-based and handler-local-alias writes are not lvalues), and bindability is declared by the CHILD while frameless emits one module at a time from one EnrichedIR. Do not treat it as a formatting choice."
      - "GATE 6 DEFERRED: every Svelte sugar ruling is deferred-not-denied until this board lands a lane at a pinned lockfile version."
      - "If this board closes IR-4 it must do so as a GENERAL facility, not a Svelte-shaped hack - Vue needs it for defineModel (3.4+) and Angular for signal input()/output() (>=17.1/17.2)."
      - "poc/09-storage/svelte exists from earlier work and took cheap lexical sugar with classic lifecycle shapes. Prior in-repo assumption, not validated decision."
      - "CORRECTED 2026-07-27 (T010 revalidation): pnpm check DOES now typecheck emitted output. NOT a gap. Svelte complication: tsc cannot parse .svelte AT ALL, so no emitted-typecheck twin is possible. NARROWED BY T002 RULING 6 - svelte-check is NOT required at T003 and IS required at T004 as the scaffold's own unmodified check script. Reason: T003's corpus is three self-contained single-file components with no cross-module props, so the class svelte-check adds beyond compile() has a population of ZERO there, and buying an instrument for a population of zero is how this repo acquired instruments whose preconditions were never asserted. T003 uses svelte/compiler compile() warnings asserted as an EXACT EMPTY SET with a mutation calibration. RE-OPEN TRIGGER: svelte-check becomes required the moment the lane emits a module consuming another emitted module, or a route passes props to an emitted component."
      - "CORRECTED 2026-07-27: pnpm test:browser does NOT hang - it runs clean, react 55/55 and solid 44/44. STILL TRUE: pnpm e2e pins dev mode only (scenarios.box.ts modes [dev])."

rules:
  pm_owns_state: true
  one_active_task: true
  max_write_workers: 1
  no_implementation_without_worker_or_pm_task: true
  no_completion_without_judge_or_pm_audit: true
  planning_is_not_completion: true
  queued_required_worker_blocks_completion: true
  continuous_until_full_outcome: true
  missing_input_or_credentials_do_not_stop_goal: true
  preserve_and_validate_existing_plan: true
  intake_misfire_must_be_audited: true
  goal_pressure_requires_oracle: true
  no_completion_on_weak_proof: true
  slice_policy:
    max_consecutive_tiny_tasks: 2
    prefer_vertical_slices: true
    judge_picks_largest_safe_slice: true
    worker_completes_whole_slice: true

agents:
  scout: installed
  worker: installed
  judge: installed

visual_board:
  selected: unknown
  local:
    status: not_requested
    url: null
    command: "npx goalbuddy board docs/goals/frameless-svelte-v1"

active_task: T003

tasks:
  - id: T001
    type: scout
    assignee: Scout
    status: done
    reasoning_hint: medium
    objective: "Map the official Svelte scaffold, its activation model, and the exact version boundaries that make IR-4 load-bearing."
    inputs:
      - "The current official Svelte scaffold (npx sv create) - contents and pinned versions"
      - "Svelte 5 docs and source: runes, event attributes, the removal of modifiers, $bindable, snippets"
      - "scripts/e2e.mjs - the activation matrix and its vocabulary"
      - "demos/react-official and demos/solid-official - the shape a fourth official demo must match"
      - "packages/compiler/src/schema.ts - IR-1, IR-3, IR-4, IR-5, IR-6, IR-7 as they stand"
      - "poc/09-storage/svelte - prior in-repo assumptions"
    constraints:
      - "Read-only. Do NOT scaffold anything yet."
      - "Enumerate the VERSION BOUNDARIES precisely: which construct needs which minimum Svelte version, and what the scaffold actually pins. This is the evidence IR-4 will be ruled on."
      - "Report how Svelte 5's removal of event modifiers interacts with IR-5's preventDefault|stopPropagation - specifically what the emitter must generate instead."
      - "Report whether svelte-check covers what pnpm check does not."
    expected_output:
      - "Official scaffold contents and pinned versions"
      - "Svelte's activation model and whether the e2e vocabulary covers it"
      - "Version boundary table: construct -> minimum version -> what the scaffold pins"
      - "How IR-5's two modifiers must be emitted with modifiers removed in Svelte 5"
      - "Whether svelte-check covers emitted output"
      - "Which IR gaps block S1/S2/S3 versus only sugar"
    receipt:
      result: done
      harness: claude-code
      evidence:
      - "github.com/sveltejs/cli packages/sv/src/create/templates/minimal/package.template.json - the pinned versions"
      - "packages/sv/src/create/shared/+typescript/package.json - svelte-check ^4.6.0 and the check script"
      - "scripts/e2e.mjs:26-33 officialDemos, :97 the activation equality throw"
      - "demos/react-official/three-way-contract.ts - the closed Activation union, assertServedActivation, ACTIVATION_MARKER"
      - "demos/{react,solid}-official/package.json - the hand-wired vite+express shape"
      - "packages/compiler/src/schema.ts:35-39, :318-325, :493-515 - IR-5, syncPolicy, and the absent version field"
      - "packages/frameworks/qwik/src/emitter/index.ts:487 hoistsPreventDefault - the WHEN/WHAT vs WHERE precedent"
      - "packages/frameworks/solid/test/emitted-typecheck.test.ts - the exact-equality ACCEPTED-diagnostics pattern"
      - "sveltejs/svelte CHANGELOG - 5.16.0 clsx class, 5.20.0 $props.id(), 5.25.0 writable deriveds, 5.29.0 attachments"
      - "poc/09-storage/svelte/App.svelte - the prior in-repo assumption"
      - "pnpm-workspace.yaml:16 - vite ^8.0.16, matching the scaffold pin"
  note_correction: "The T001 receipt originally cited notes/T001-scaffold-and-versions.md, which was never written. Caught by T002. The inline receipt below IS the record; the citation is withdrawn rather than back-filled, because writing the note now would be reconstructing evidence after the fact. A receipt citing evidence that is not there is the board-level form of instrument rule 2."
      summary: "Mapped the sv@0.16.6 minimal scaffold: SvelteKit 2.63 / svelte ^5.56.1 / vite ^8.0.16 - the SAME vite as the root catalog, so Svelte does not repeat Qwik's vite-pinning problem. Svelte hydrates, so e2e needs no third activation term. Every construct S1/S2/S3 needs is Svelte 5.0 baseline, so IR-4 blocks no lane. IR-5 needs no schema change - modifiers were removed, so the actions emit as ordinary in-body statements; the live question is DELEGATION, not modifiers."
      scaffold:
        command: "npx sv create, sv@0.16.6, minimal + TypeScript"
        pins: "@sveltejs/kit ^2.63.0, svelte ^5.56.1, @sveltejs/vite-plugin-svelte ^7.1.2, vite ^8.0.16, svelte-check ^4.6.0, @sveltejs/adapter-auto ^7.0.1"
        vite_alignment: "^8.0.16 is byte-identical to the root catalog (pnpm-workspace.yaml:16). The cheapest good news on the board - no Qwik-style vite-7.3.1 pinning problem."
        notable: "The minimal template has NO svelte.config.js; the adapter is configured inline in vite.config.js. Its own +layout.svelte is ALREADY runes plus snippets - `let { children } = $props()` and `{@render children()}` - so snippets are the scaffold's default-children mechanism, not optional sugar."
      activation:
        model: "SvelteKit SSRs then hydrates. activation: 'hydrate' already exists; NO third e2e term needed. The Activation union in three-way-contract.ts is closed on framework names and needs 'svelte' added to the hydrate arm - a widening, not a weakening."
        friction_1: "The hydrate branch asserts the served HTML contains the literal '/src/entry-client.jsx'. SvelteKit owns its client entry and instead emits an inline __sveltekit_* bootstrap importing a kit-generated module. Making the literal per-demo keeps the assertion specific; relaxing it to 'any module script' would weaken it. JUDGE CALL."
        friction_2: "There is no demo-owned entry-client to set data-frameless-activated. The nearest scaffold-preserving sites are a root +layout.svelte $effect/onMount or src/hooks.client.ts. Both are scaffold-shaped; neither is a hand-rolled harness."
      structural_tension_requiring_a_ruling: "react-official and solid-official each HAND-WIRE server.js + ssr-handler.js + express, with a frameless-demo-ssr vite plugin injected by scenarios.box.ts. SvelteKit's own plugin already does SSR, so demos/svelte-official would have NO server.js and NO ssr-handler.js, and its routes become src/routes/+page.svelte etc. 'Official scaffold only' and 'match the shape of the existing three demos' are in DIRECT TENSION here and one must yield. The charter's own constraint points at SvelteKit."
      version_boundary_table:
        at_or_below_5.0_baseline: "runes $state/$derived/$effect/$props, onclick={} event attributes, on() from svelte/events, $bindable(), snippets {#snippet}/{@render}"
        above_baseline_but_satisfied_at_5.56: "<svelte:boundary> 5.3, class attribute as object/array via clsx 5.16.0, $props.id() 5.20.0, writable/reassignable $derived 5.25.0, attachments {@attach} 5.29.0"
        unavailable: "Event modifiers (on:click|preventDefault) - 3.x/4.x ONLY, removed in 5.0. The on:click DIRECTIVE survives but is deprecated in 5 and slated for removal in 6."
        ir_4_consequence: "At ^5.56.1 EVERY version-gated Svelte construct under discussion is already above its floor, so the Svelte lane produces NO FORCING CASE for IR-4. The policy's version corollary needs both 'lockfile pins >= N' AND 'the emitter can know the version'; Svelte satisfies the first and not the second. Whether the first alone suffices is exactly the T002 question - and Svelte hands the Judge the EASY end of it, which is worth saying plainly because Vue's defineModel (3.4+) and Angular's signal input()/output() (>=17.1/17.2) may not have the same slack."
      ir_5:
        no_schema_change: "Svelte 5 removed modifiers, so there is no |preventDefault spelling to target. Svelte 5 event attributes receive the native DOM Event, so the emitter generates exactly what React and Solid already generate: onclick={(event) => { event.preventDefault(); ...rest }}. No lowering, no vocabulary extension, for S1/S2/S3."
        the_real_question_is_delegation: "Svelte 5 delegates a fixed event set (click, input, change, keydown...) to the root, so a stopPropagation inside a delegated handler may not stop what an author expects; Svelte's docs direct to on() from svelte/events. That is PRECISELY the repair already recorded in idiom-policy worked example 6 - narrow the attribute-form domain to events whose declared actions exclude stopPropagation, and route the rest through on()."
        what_not_to_assume: "The Qwik failure was QRL LAZINESS - the handler arrived ~58ms after dispatch - NOT delegation. A Svelte delegated listener is in the bundle and runs synchronously during dispatch, so preventDefault from it should still avert the default action. That reasoning is DOCUMENTARY. Per instrument rule 1 it must be MEASURED on the S3 cancel-submit before any claim is filed either way."
      blocking_vs_sugar:
        blocks_s1_s2_s3: "None of IR-1, IR-3, IR-4 or IR-6."
        needs_a_ruling_but_blocks_nothing_structurally: "IR-5, to get S3 right."
        the_sleeper: "IR-7 is the only gap with real S1/S2/S3 exposure. Purity is never asserted - ExpressionSite.expression is an arbitrary AST node - while $derived expects a pure re-runnable expression, and Svelte reports violations as state_unsafe_mutation / effect_update_depth_exceeded, SOME OF THEM DEV-ONLY. S1's derived is trivial so it will pass, WHICH IS EXACTLY WHY this is the gap most likely to be silently wrong later."
        sugar_only: "IR-1 ($bindable/bind:, explicitly out of scope), IR-3 (snippets beyond the default slot - TemplateDefaultSlotProjection maps cleanly onto {@render children?.()}, so IR-3 only bites if the lane extends to the composition kit), IR-4, IR-6."
      svelte_check: "YES, materially beyond the shared lane, and the shared lane CANNOT substitute: tsc cannot parse .svelte at all, so a packages/frameworks/svelte emitted-typecheck twin would have nothing to read. svelte-check adds invalid rune usage, state_referenced_locally, non-reactive derived misuse, invalid {@render} targets, unknown component props, wrong handler signatures. Cost: needs svelte-kit sync plus a project tsconfig, and emits warnings the Solid lane's exact-equality ACCEPTED pattern would have to absorb. Cheaper independent oracle worth weighing: svelte/compiler's compile() returns a warnings array with no project scaffolding at all."
      contradictions_found:
        confirmed_and_fixed_by_pm: "state.yaml still carried the two STALE standing gaps verbatim while goal.md had been corrected - and state.yaml is declared to WIN. This was a PM miss in T010: correction fields were added without amending the original existing_plan_facts entries. Corrected in all THREE adapter boards, not just Svelte."
        confirmed_needs_ruling: "'Official scaffold only' versus 'match the existing demo shape' cannot both hold for SvelteKit."
        NOT_confirmed: "The Scout reported worked example 9 duplicated line-for-line in docs/emitter-idiom-policy.md. PM CHECKED: it is not - a single occurrence of the heading and of the 'Sanctioned set, stated first' paragraph. Instrument rule 1 applies to Scouts too; recorded so the claim does not propagate."
      ambiguity_requiring_judge:
        - "IR-4: does 'the lockfile pins >= N' satisfy the version corollary on its own, or must the emitter be handed the version? Svelte cannot force the answer and gives the easy end; rule knowing Vue and Angular inherit it."
        - "IR-5 emission: in-body statements for both actions, versus worked example 6's repair routing stopPropagation-carrying events through on(). Needs MEASUREMENT of Svelte 5 delegation first."
        - "Whether widening the Activation union and making the served-payload literal per-demo counts as weakening an activation-neutrality check. T999 is instructed to reject on exactly this, so rule up front rather than defending after."
        - "Whether demos/svelte-official is a SvelteKit app (official, structurally unlike the other three) or a bare vite + plugin-svelte SSR setup mirroring react-official (structurally matching, but hand-rolled - the exact thing frameless-qwik-v1 exists to forbid)."
      disclosed: "Used 18 commands against a 12-command budget. The task named eight separate inputs plus upstream version verification."
  - id: T002
    type: judge
    assignee: Judge
    status: done
    reasoning_hint: high
    objective: "Ratify the Svelte adapter contract and rule on whether IR-4 is closed by this board or deferred."
    inputs:
      - "T001 receipt"
      - "docs/emitter-idiom-policy.md - especially Gate 6's version corollary"
    constraints:
      - "Do not implement."
      - "Rule on IR-4 EXPLICITLY. If closed, the design must be a general facility usable by the Vue and Angular boards - not a Svelte-shaped hack. If deferred, state the consequence for those boards."
      - "Rule on how IR-5's two modifiers are emitted given Svelte 5 removed modifiers."
      - "Separate what S1/S2/S3 activation neutrality requires from what Svelte idiom sugar would be nicer with."
      - "Do NOT rule bind: into scope. It is different-in-kind on two axes and its failure mode is a dev-only console warning; if it is wanted, it needs its own package with its own proof."
    expected_output:
      - "IR-4 ruling: closed here (with design) or deferred (with consequence)"
      - "IR-5 emission ruling"
      - "S1/S2/S3 scope for the first Worker package"
      - "Exact Worker objective, allowed_files, verify, stop_if"
    receipt:
      result: done
      harness: claude-code
      decision: approved
      full_outcome_complete: false
      rationale: "The demo-shape tension is SMALLER than T001 stated: demos/qwik is already a stock official scaffold with no server.js, no express and no frameless-demo-ssr plugin, so SvelteKit follows a landed precedent rather than breaking a three-demo uniformity. Union widening approved as a REQUIRED per-demo field, not a relaxation. IR-4 deferred: Svelte produces no forcing case, and a general facility designed against zero forcing cases is folklore. IR-5 needs measurement, not documents - and the corpus contains ZERO stopPropagation, which relocates the real risk."
      ruling_1_demo_shape:
        verdict: "demos/svelte-official IS a SvelteKit app from the official scaffold."
        the_tension_was_weaker_than_reported: "'Match the shape of the existing three demos' is not in the charter, and it is ALREADY FALSE. demos/qwik has no server.js, no ssr-handler.js, no express and no vite plugin overlay - its scenarios.box.ts says verbatim 'No config overlay and no middleware: qwikRouter() already renders on the server in dev. This is the stock pnpm create qwik pipeline.' Only 2 of 3 demos hand-wire SSR, and the third - landed by the goal that exists to FORBID hand-rolled harnesses - is structurally identical to what SvelteKit needs."
        what_must_actually_match: "Not the server wiring. The CONTRACT PROTOCOL: scenarios.box.ts importing three-way-contract.ts, assertServedActivation + runScenario over scenarioIds, the JSON three-way-results receipt note, a copy-emitted script, and an officialDemos row in scripts/e2e.mjs."
        cost_of_the_losing_constraint: "Three, stated so they are not discovered later. (a) Emitted components are wired through SvelteKit's route conventions, so a convention change breaks this lane in a way react/solid are immune to. (b) No shared createSsrHandler between pnpm dev and the box, so react-official's 'no second harness exists to drift' property is unobtainable - SvelteKit's own dev server is the single path instead, equivalent by a different argument. (c) modes:['dev'] means the vite build + adapter path is never exercised, identical to the still-true standing gap for qwik."
      ruling_2_activation_union:
        verdict: "Widening APPROVED, does not weaken activation neutrality - conditionally."
        analysis: "assertServedActivation's hydrate arm makes THREE claims. The two negatives - forbidInServedPayload(['q:container', ACTIVATION_MARKER]) - are framework-agnostic and ARE the neutrality claims; they do not change. The positive, contains '/src/entry-client.jsx', is NOT a neutrality claim; it is a LANE-IDENTITY claim, shared by react and solid only because both scaffolds happen to name their entry identically. Parameterising it per demo preserves the identical claim at each demo's own literal. Relaxing it to 'any module script' would weaken it and is REJECTED."
        conditions:
          - "The literal must be a REQUIRED field on the hydrate arm (e.g. readonly servedClientEntry: string) so the type forbids a lane omitting it. An optional field is a silent opt-out and is rejected."
          - "The Svelte literal must be MEASURED from what SvelteKit dev actually serves, not guessed, and must be a framework-owned module string."
          - "Instrument rule 3: the chosen literal must be SHOWN able to go red - a recorded run pointed at a string the payload does not contain, or a negative control. A served-payload literal never observed failing is not a check."
          - "The activation marker must be set in Svelte's OWN post-mount signal - onMount in the root +layout.svelte - NOT hooks.client.ts init, which runs before hydration completes. Imperative, never a template attribute binding, or SSR would emit it and forbidInServedPayload would correctly fail."
        mechanical_test_for_T999: "For react, solid and qwik, the SET of assertions run and the exact strings asserted must be byte-unchanged after the Svelte lane lands. Moving react's literal from a helper constant into its own activation object is not a weakening; changing, softening or making it optional is."
        forward_note_for_T004: "runScenario computes navigations = resume ? 1 : 0. If SvelteKit's client router records a history entry on initial load (qwik's does), the permitted repair is the SAME KIND of widening - a per-demo declared expected count, still asserted exactly. Relaxing to 'any number' is not permitted. Decide by measurement."
      ruling_3_ir4:
        verdict: "IR-4 DEFERRED. The version corollary is NOT amended - both conjuncts stand. 'The lockfile pins >= N' alone does NOT satisfy it."
        why_deferred: "Svelte produces no forcing case; at ^5.56.1 every construct is above its floor. Designing a general facility against ZERO forcing cases is the Gate 4 error transposed up to the IR schema. And generality is undecidable today - neither the Vue nor Angular board has run, so the SHAPE is unknown: a version string per target, a lockfile-resolved map, or (probably right, unprovable today) a CAPABILITY set, since emit sites want 'supports reassignable-derived', not scattered semver comparisons."
        why_the_lockfile_pin_alone_is_insufficient: "Frameless is a PUBLISHED compiler - vite.config.ts packs @frameless/react and @frameless/solid, and packages/cli builds for consumers. The lockfile pin is THIS repo's test-lane version, not the consumer's framework version. An emitter assuming its own dev repo's pin will emit 5.25+ syntax into a consumer app on 5.10. That is the instrument-fault pattern verbatim - measuring through a proxy whose stability the product never promised - moved from tests into SHIPPED OUTPUT. The two conjuncts answer different questions: 'can a regression be caught here?' and 'is the emitted output valid there?'. Collapsing them from the comfortable case is the trap T001 named."
        the_second_conjunct_is_satisfiable_two_ways: "(a) an explicit target-version or capability input on EnrichedIR, or (b) an emitter that emits ONLY baseline-version-safe forms, in which case there is no version-gated sugar and the corollary never engages. Svelte is (b) today. Recording (b) explicitly is what lets T003 proceed without IR-4 and lets T005 resolve honestly rather than by shrug."
        binding_on_vue_and_angular: "Neither may invent a local answer. Both inherit: IR-4 deferred, emit baseline-safe forms, record 'DEFERRED - no target-version input' at Gate 6 for any version-gated sugar."
        reopen_trigger: "Falsifiable, not 'someday': the first construct where (i) the baseline-safe form is materially worse AND an official scaffold pins below the sugar's floor, or (ii) frameless ships to a consumer whose framework version differs from this repo's lockfile."
      ruling_4_ir5:
        schema: "NO change. Svelte 5 removed modifiers and its event attributes receive the native DOM Event, so the actions emit as ordinary in-body statements exactly as React and Solid already emit them."
        measurement_is_mandatory: "Gate 1 records FAIL when the only evidence is documentary AND a build of the target framework is in the lockfile - and T003 is the package that puts Svelte there. T001 was right to call its own reasoning documentary."
        the_risk_is_not_where_T001_put_it: "VERIFIED against the goldens: s1/s2/s3 contain ZERO occurrences of stopPropagation (preventDefault 4x, syncPolicy 2x, all in s3). Worked example 6's G4 counterexample has NO INSTANCE in T003's corpus. The risk that actually bites is different: (i) preventDefault() inside a DELEGATED click handler on a <button type=submit> must still avert the real GET navigation assertS3 checks by Document-request count; and (ii) S3's emitted output puts an onClick on the FORM ITSELF reading event.target.dataset.action === 'submit', so the lane depends on a click BUBBLING from button to form. Under Svelte 5 both are delegated to the root and propagation is simulated. That pair is the measurement."
        form: "Emit the attribute form for every event pending measurement, and do NOT build the on() path - with zero stopPropagation in the corpus that branch would be untested dead code, which in an emitter is worse than absent code. Instead FAIL CLOSED: throw on any actions containing stopPropagation, with a gate row proving the throw fires. If measurement shows the attribute form unsound for preventDefault, switch the WHOLE component to on() - never a mix."
        consequence_for_T005: "This leaves worked example 6 honestly deferred with something real to decide. T003+T004 meet all three deferring conditions, so T005 re-runs all six gates against a real emitter and a real domain. Had T003 pre-adopted the sugar, T005 would be ratifying a fait accompli."
      ruling_5_scope_and_gaps: "S1/S2/S3 only. IR-1 out of scope hard. IR-3 defer (no projection in the corpus). IR-4 deferred. IR-5 no schema change. IR-6 defer (no instance). IR-7 IS THE SLEEPER: do not attempt a purity proof - ship a fail-closed CONSERVATIVE syntactic guard on the emitter's own output, plus a mutation row proving it goes red. This converts 'silently wrong later' into 'loudly wrong at emit time' for the decidable subset and documents the undecidable remainder."
      ruling_6_svelte_check:
        verdict: "NOT required at T003. svelte/compiler compile() warnings ARE required, asserted as an EXACT EMPTY SET with a mutation calibration. svelte-check IS required at T004, but only as the scaffold's own unmodified check script."
        why: "T003's corpus is three self-contained single-file components with no cross-module props. The class svelte-check adds beyond compile() - unknown component props, wrong handler signatures, invalid {@render} targets - has a population of ZERO there, and buying an instrument for a population of zero is how this repo acquired instruments whose preconditions were never asserted. compile() also runs in-process against the exact version the package resolves, satisfying Gate 1; svelte-check via svelte-kit sync would couple the check to the DEMO's SvelteKit install, which Gate 1 explicitly calls a different build."
        conflict_disclosed: "This NARROWS a state.yaml fact, and state.yaml is declared to win. Overruled deliberately and recorded, not silently superseded. RE-OPEN TRIGGER: svelte-check becomes required the moment the lane emits a module consuming another emitted module, or a route passes props to an emitted component."
      finding_7_dev_warnings_unenforceable:
        severity: high
        evidence: "@async/witness 0.7.0 exposes consoleErrors ONLY (index.d.mts:501). PageHandle (:163-190) has NO console-message accessor at all. runScenario asserts consoleErrors: 0."
        consequence: "T004's constraint 'a dev-only console warning is a FAILURE here' CANNOT BE ENFORCED by the demo lane as instrumented, and T999's 'reject if dev-only warnings were observed and dismissed' would pass VACUOUSLY, because nothing observes them. Svelte reports state_unsafe_mutation and ownership_invalid_mutation as console.warn."
        ruling: "Enforce where the instrument reaches: T003's vitest browser lane has direct console access and must fail on ANY Svelte dev warning, not only errors. T004 must then either land a console.warn capture sink - the ONLY permitted scaffold addition beyond wiring emitted components - or record IN WRITING that the demo lane cannot observe warnings and that T003's lane is the sole enforcement point. Silence is not an option."
      pm_actions_taken: "T003 UNBLOCKED: the PM created packages/frameworks/svelte/package.json and ran pnpm install, so svelte resolves to 5.56.8 on the recorded ^5.56.1 line and the sandbox needs no network. The svelte-check fact and the phantom T001 note citation were both corrected in state.yaml rather than left alongside a contradiction."
      dissent:
        - "The Judge believes the attribute form will prove SOUND for preventDefault at 5.56 - a delegated listener is in the bundle and runs during dispatch, unlike a lazily fetched QRL - and more likely WRONG for stopPropagation, since a delegated child handler cannot stop an ancestor running from the root under simulated propagation. Cannot evidence either without T003's measurement, and Gate 1 forbids acting on it. Written down so a contradiction is visible rather than quietly absorbed."
        - "The Judge believes Vue's defineModel is DENIED rather than deferred - Gate 2 fails on child-declared bindability, and FAIL outranks DEFERRED - which would mean IR-4 was never its blocker. A prediction for the Vue board to test, not a finding."
        - "The Judge suspects IR-4 may NEVER acquire a forcing case under an official-scaffold-only policy, since official scaffolds pin recent versions by construction, and that the eventual correct design is a declared CAPABILITY FLOOR rather than a target version. Deferring rather than designing on that suspicion is the point."
        - "Not verified that root +layout.svelte onMount fires strictly after hydration completes for the whole tree at this version. Best available candidate and strictly better than hooks.client.ts, but documentary. If S1/S2/S3 flake at T004, look here first rather than raising a timeout."
  - id: T003
    type: worker
    assignee: Worker
    status: active
    reasoning_hint: high
    spec: "T002 receipt on this board - AUTHORITATIVE. Seven rulings."
    objective: >-
      Land packages/frameworks/svelte as a whole vertical slice for S1/S2/S3: an emitter turning the
      three existing compiler goldens into Svelte 5 runes single-file components, a gate with a
      mutation corpus, checked-in .svelte goldens with a byte-equality freshness test, a
      svelte/compiler compile() warnings oracle asserted as an EXACT EMPTY SET with a mutation
      calibration, and a browser lane that drives the emitted output, fails on any Svelte dev
      console WARNING as well as errors, and MEASURES the two delegation questions before the
      event-emission form is chosen.
    the_two_measurements:
      - "Does preventDefault() inside a DELEGATED click handler on a <button type=\"submit\"> still avert the real GET navigation that assertS3 checks by Document-request count?"
      - "Does a delegated handler on the <form> still observe the click BUBBLED from the button, with event.target naming the button? S3's emitted output puts an onClick on the form itself reading event.target.dataset.action === 'submit', so the corpus depends on cross-element bubbling."
    allowed_files:
      - packages/frameworks/svelte/package.json
      - packages/frameworks/svelte/tsconfig.json
      - packages/frameworks/svelte/vitest.config.ts
      - packages/frameworks/svelte/vitest.node.config.ts
      - packages/frameworks/svelte/README.md
      - packages/frameworks/svelte/src/index.ts
      - packages/frameworks/svelte/src/emitter/index.ts
      - packages/frameworks/svelte/src/emitter/estree.ts
      - packages/frameworks/svelte/src/format-emitted.ts
      - packages/frameworks/svelte/src/gate/index.ts
      - packages/frameworks/svelte/scripts/regenerate.ts
      - packages/frameworks/svelte/generated/S1.svelte
      - packages/frameworks/svelte/generated/S2.svelte
      - packages/frameworks/svelte/generated/S3.svelte
      - packages/frameworks/svelte/test/emitter.test.ts
      - packages/frameworks/svelte/test/gate.test.ts
      - packages/frameworks/svelte/test/compile-emitted.test.ts
      - packages/frameworks/svelte/test/emitted-smoke.browser.test.ts
      - packages/frameworks/svelte/test/setup.ts
      - packages/frameworks/svelte/.gitignore
      - vite.config.ts
      - package.json
      - docs/goals/frameless-svelte-v1/notes/T003-svelte-emitter.md
    pm_preinstall_done: >-
      UNBLOCKED 2026-07-27. The PM created packages/frameworks/svelte/package.json and ran pnpm
      install. svelte resolves to 5.56.8, on the ^5.56.1 line T001 recorded. @sveltejs/vite-plugin-svelte,
      vite and vitest are on catalog entries. The Worker may edit only the scripts block;
      pnpm-lock.yaml MUST NOT move.
    verify:
      - "pnpm check"
      - "pnpm test"
      - "pnpm --dir packages/frameworks/svelte exec vitest run --config vitest.config.ts --project svelte-browser"
      - "pnpm test:browser"
      - "pnpm e2e"
      - "node packages/frameworks/svelte/scripts/regenerate.ts && git diff --exit-code -- packages/frameworks/svelte/generated   # goldens are fresh"
      - "git diff --exit-code -- packages/frameworks/react packages/frameworks/solid packages/frameworks/qwik packages/compiler demos scripts   # nothing outside the new package moved"
      - "git status --short   # every path inside allowed_files; pnpm-lock.yaml MUST NOT appear"
    stop_if:
      - "Need files outside allowed_files."
      - "pnpm-lock.yaml moves, or any dependency needs installing. The sandbox has no network. Stop and report the exact package and version; the PM installs before re-dispatch."
      - "The svelte version packages/frameworks/svelte resolves is not the ^5.56.1 line. Gate 1 would then have two answers. Stop and report. (PM measured 5.56.8.)"
      - "You are about to emit bind: or $bindable. Out of scope on two independent axes, and its failure mode is a DEV-ONLY console warning that passes tests."
      - "You are about to build an on()-from-svelte/events code path. VERIFIED: the s1/s2/s3 goldens contain ZERO stopPropagation, so that path would be untested dead code - which in an emitter is worse than absent code. Emit the attribute form and make stopPropagation THROW at emit time instead, with a gate row proving the throw fires."
      - "You are about to emit a MIX of delegated onclick= attributes and on() listeners inside one component. S3 depends on a click bubbling from [data-action=\"submit\"] to the form's own handler; mixing mechanisms changes ordering. All or nothing."
      - "You are about to choose the event-emission form on DOCUMENTARY evidence. Gate 1 records FAIL for documentary-only evidence once the framework is in the lockfile, and this package puts it there. Measure both delegation questions in a browser first."
      - "A gate policy, the compile() empty-warnings assertion, or the console-warning check cannot be made to go RED. A check that cannot fail is not a check."
      - "Any mutation in the gate corpus is built with a bare .replace(). Copy the throwing mutate/mutateAll constructor from packages/frameworks/qwik/test/gate.test.ts - its own doc comment instructs a new adapter to do exactly this. Audited corpus-wide by defects-and-targets T018."
      - "You are about to add a formatter dependency (prettier, prettier-plugin-svelte) or run oxfmt over a .svelte file. Ruled: emit deterministic text directly and record the divergence from the other three adapters in the note."
      - "You are about to create packages/frameworks/svelte/src/adapter.ts. Out of scope: nothing consumes it, and a fourth byte-equivalent copy of the quiescence loop T017 just repaired in three places is drift with no consumer."
      - "You are about to touch packages/compiler/src/** or its goldens. IR-4, IR-5 and IR-7 are all ruled: no schema change in this package."
      - "A construct S1/S2/S3 needs is above the pinned Svelte version. T001 says none are; if you find one, that REFUTES T001 and is a finding, not a workaround."
      - "Verification fails twice for the same reason."
    constraints:
      - "IR-7, THE SLEEPER - do not let it pass silently. S1's derived is trivial so it WILL pass, which is the definition of a green vacuum. Do NOT attempt a purity proof (compiler-wide, no forcing case). DO ship a fail-closed CONSERVATIVE guard: a gate policy rejecting an emitted $derived(...) whose expression contains an assignment, an update expression, or a call in the emitter's known-mutating set - a syntactic check on the emitter's OWN output, with a comment naming IR-7 and stating plainly what the check cannot see. Plus one mutation row proving it goes red."
      - "DEV WARNINGS: T002 finding 7 established that the witness API cannot observe console warnings at all - PageHandle has no console accessor and PageOutcomeExpectation exposes consoleErrors only. So the demo lane CANNOT enforce the dev-warning constraint and T999's version of it would pass vacuously. Your vitest browser lane HAS direct console access and is therefore the sole enforcement point: fail on ANY Svelte dev warning, not only errors."
      - "IR-4 is DEFERRED and the version corollary is NOT amended. Emit only baseline-version-safe forms; every S1/S2/S3 construct is Svelte 5.0 baseline."
      - "No schema change, no bind:, no demo, no e2e row. T004 owns the demo and the contract widening."
    receipt: null

  - id: T004
    type: worker
    assignee: Worker
    status: queued
    reasoning_hint: medium
    objective: "Stand up demos/svelte-official from the official scaffold and add the Svelte row to the e2e activation matrix, asserting S1/S2/S3 equality."
    allowed_files: []
    verify: []
    stop_if:
      - "The official scaffold needs modification beyond wiring in emitted components."
      - "Equality cannot be asserted without weakening an existing assertion."
      - "Verification fails twice."
    constraints:
      - "Official scaffold as it ships."
      - "Do not weaken any existing activation-neutrality check."
      - "A dev-only console warning is a FAILURE here, not noise - Svelte reports ownership violations that way."
    receipt: null

  - id: T005
    type: judge
    assignee: Judge
    status: queued
    reasoning_hint: high
    objective: "With a Svelte lane at a pinned version, re-run the idiom policy on at least one version-gated Svelte sugar question and record whether the version corollary now resolves."
    inputs:
      - "docs/emitter-idiom-policy.md worked example 6 (Svelte onclick, currently deferred after repair)"
      - "The landed Svelte lane and the IR-4 ruling"
    constraints:
      - "Do not implement. Rule."
      - "This is the direct test of the policy's version corollary. If IR-4 was closed, a version-gated sugar should now resolve; if it was deferred, say plainly that it still cannot and why."
      - "Record the ruling in the policy's own format so it can be folded back into docs/emitter-idiom-policy.md."
    expected_output:
      - "Six gate outcomes for at least one version-gated Svelte sugar question"
      - "Whether the version corollary resolved, with evidence"
      - "Whether docs/emitter-idiom-policy.md needs updating"
    receipt: null

  - id: T999
    type: judge
    assignee: Judge
    status: queued
    reasoning_hint: high
    objective: "Audit whether the Svelte lane satisfies the oracle and the full owner outcome."
    inputs:
      - "All done task receipts"
      - "Last pnpm e2e output"
      - "Current dirty diff"
    constraints:
      - "Do not implement."
      - "Reject completion if the Svelte row exists but does not ASSERT equality with the other demos."
      - "Reject completion if any existing activation-neutrality check was weakened."
      - "Reject completion if IR-4 received no explicit ruling either way."
      - "Reject completion if dev-only console warnings were observed and dismissed."
    expected_output:
      - "complete | not_complete"
      - "full_outcome_complete: true | false"
      - "missing evidence"
      - "next task if not complete"
    receipt: null

checks:
  dirty_fingerprint: unknown
  last_verification:
    result: unknown
    task: null
    commands: []

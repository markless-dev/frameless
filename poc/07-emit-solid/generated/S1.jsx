// @generated by arcade-poc-07-emit-solid; do not edit.
import { createSignal } from "solid-js";
export function RenderOnce(props) {
  props.onTrace("setup", {
    runs: 1
  });
  const [count, setCount] = createSignal(1);
  const prefix = `${props.label}:`;
  const derived = () => `${prefix}${count() * props.multiplier}`;
  return !props.visible ? <p data-branch="hidden">hidden</p> : <section data-scenario="s1"><output data-value="derived">{derived()}</output><button data-action="increment" onClick={() => {
      const nextCount = count() + 1;
      setCount(nextCount);
      props.onTrace("change", {
        count: nextCount
      });
    }}>increment</button></section>;
}
